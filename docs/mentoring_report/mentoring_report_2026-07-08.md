# 멘토링 보고 (2026-07-08)

> 작성자: 김영혜 (② RAG·가이드생성)

## 1. 지금까지 한 것

### RAG 파이프라인 구축 (`rag-prototype/`)
- 식약처 e약은요 API 연동(`mfds_client.py`) + HIRA 약가마스터 CSV 로컬조회(`hira_master.py`) 추가 — 표준코드·ATC코드·허가/취소상태로 품목 검증 보강
- 만성질환 생활지침 데이터 추가, 진단명 기반 인용 연결
- Hallucination 방어: 참고자료 강제 인용(`source_refs`), JSON 형식 강제, self-consistency(동일 질의 N회 생성 후 최다 일치 답 채택)

### OCR ↔ RAG 실제 연동 검증
- 순현님의 실제 OCR 인터페이스·실측 데이터로 직접 검증(dry-run 4건 + 실제 OpenAI 호출 2건)
- 편차 4건(D1~D4) 발견·해결
  - D1. 배치 처리 진입점 부재 → `generate_guides_from_ocr_result()` 추가
  - D2. **OCR 개별 신뢰도가 최종 응답에 반영 안 되던 버그(가장 심각, 실측 확인)** → `confidence < 0.80`이면 강제 `review_required=True`
  - D3. `review_required` 필드명 충돌 → `review_flags`로 사유 코드 구분
  - D4. 생활지침 커버리지 한계(버그 아님) → 문서화
- `CONTRACT.md` 신설(OCR↔RAG 필드 계약 정본), 회귀테스트 4건 + 계약테스트 2건 추가

### dev 반영 (PR #11, 머지 완료)
- RAG 파이프라인 + 챗봇 GPT 연동 — `RAG_PROVIDER=real` 전환, `/chat/ask`가 환자의 최근 처방·가이드 결과를 참고해 GPT로 개인화 답변 생성

### 버그 수정 / 정리
- `RAG_PROVIDER` stub↔real 전환 시 `Result.tsx`/`PrescriptionDetail.tsx` 크래시 나던 것 수정 → PR #13 (리뷰 대기, 소정님 브랜치 위로 재작성하며 중복이던 PR #12는 닫음)
- HIRA 약가마스터 CSV(54MB)가 `rag-prototype/data`·`backend/data` 두 곳에 중복 보관되던 것을 `backend/data` 한 곳으로 통일 → PR #14 오픈

### dev ↔ sojung 브랜치 병합 검증 (오늘)
- 소정님 브랜치(`feature/frontend-setup_sojung`)가 이미 자체적으로 `dev`를 병합해둔 상태(`0bb7e57`)를 확인 — 애초 우려했던 "브랜치가 서로 다른 걸 모른 채 갈라짐" 문제는 소정님 쪽에서 이미 해소됨
- 실제 병합을 로컬(별도 worktree, 원격에 올리지 않음)에서 시도해 **충돌 없이 깨끗하게 병합됨**을 확인
- 병합 시 sojung 쪽 커밋이 `backend/data/hira_drug_master_20251031.csv`를 독자적으로 추가해 PR #14와 같은 중복이 다시 생기는 것을 발견 → 체크섬 동일 확인 후 동일한 방식으로 정리, `pytest` 29/29 통과 + 백엔드 앱 임포트 정상 확인까지 마침
- **PR로 올리지는 않은 상태** — 실제로 언제/누구 명의로 병합할지는 아래 문의사항 ②, 그리고 팀 업무분담(별첨 md2)에 따라 결정 예정

## 2. 앞으로 해야 할 것

1. dev ↔ sojung 병합을 실제로 반영 (검증은 끝났고, 방향만 확정되면 바로 실행 가능)
2. PR #13, #14 리뷰 반영 및 머지
3. (로드맵 2주차) 벡터DB 본격 구축 확정, 프롬프트 v2, OCR 결과 연동 테스트 계속
4. (로드맵 3주차 예정) 생활습관가이드 커버리지 확장(REQ-004) — 현재 고혈압·당뇨병·이상지질혈증·만성콩팥병 4개 질환만 커버, 동일입력 결과편차 테스트(평가기준 3-3)

## 3. 멘토님께 문의드릴 내용

### ① 민감정보 보호(익명화/가명화) 구축 필요 여부
현재 `Patient.name`(환자 실명), 처방전 원본 이미지 경로, OCR 원문, 진단명·복용약물이 전부 평문으로 DB에 저장되고 있고, 코드 전체를 확인해봐도 암호화·가명처리·익명화 로직이 전혀 없는 상태입니다. 이번 프로젝트 평가기준/범위에 이 정도 개인·의료정보 보호 구축이 요구되는지, 필요하다면 어느 수준(가명처리 vs 필드 암호화 vs 접근제어)까지 해야 하는지 여쭤보고 싶습니다.

### ② 업무 분담안 검토
현재 상황(소정님이 백엔드 백로그 대부분 완료, 브랜치 병합 준비 끝남, 남은 로드맵 항목들)을 반영해 업무 분담을 다시 정리했습니다(별첨 md2 참고). 이 방향대로 진행해도 괜찮을지, 일정상 조정이 필요한 부분이 있을지 확인받고 싶습니다.
