# 1주차 세부 티켓 (화·수·목·금)

> 월요일은 팀 전체 킥오프(요구사항/ERD/API명세서/와이어프레임 검토, 역할 배정 확정)로 진행하며, 개인 작업은 화요일부터 시작합니다.

## 화요일

| 시간 | ① OCR·정보추출 | ② RAG·가이드생성 | ③ 챗봇·백엔드 | ④ 프론트·배포 |
|---|---|---|---|---|
| 오전 | CLOVA OCR 콘솔 가입·키 발급, 단일 이미지 호출 테스트 | OpenAI API 키 확인, langchain/openai 패키지 설치, 공식 quickstart 1개 실행 | FastAPI 프로젝트 구조 잡기(`app/main.py`, `models/`, `routers/`), 서버 기동 확인 | Figma 새 파일 생성, 화면 5개(로그인/업로드/처리중/결과/챗봇) 박스 배치 |
| 오후 | 처방전·약봉투 샘플 5~10장 확보 후 각각 OCR 호출, 결과를 표로 기록 | 식약처 e약은요 공공데이터 API 신청 또는 약품 10건 데이터 수동 정리 | User·MedicalRecord 테이블 SQLAlchemy 모델 작성, 로컬 DB 테이블 생성 | 화면 간 이동 흐름 화살표 표시, 업로드/결과화면 레이아웃 구체화 |
| **완료기준** | 샘플별 인식 성공/실패표 완성 | API키 발급 또는 데이터 10건 확보 | 로컬 DB에 테이블 2개 생성 확인 | 화면 5개 + 흐름 1차 완성 |

## 수요일

| 시간 | ① OCR·정보추출 | ② RAG·가이드생성 | ③ 챗봇·백엔드 | ④ 프론트·배포 |
|---|---|---|---|---|
| 오전 | 추출 텍스트에서 약품명·용량·복용법 패턴 정리 시작 | FAISS 또는 ChromaDB 설치, 의약품 텍스트 임베딩 작업 | POST `/medical-records` 엔드포인트 스켈레톤(이미지 저장만) | 프론트 프로젝트 셋업(React 등), 라우팅 구조 잡기 |
| 오후 | 정리한 패턴을 REQ-002 JSON 스키마로 변환, 샘플 1건 검증 | 벡터DB에 질의 1건 → 관련문서 top-3 검색 테스트 | Swagger UI에서 업로드 테스트, 응답 형식 확정 | 업로드화면 정적 구현(버튼·업로드영역 배치) |
| **완료기준** | 샘플 1건이 JSON 스키마로 정확히 변환됨 | 질의→검색 결과 top-3 정상 출력 | Swagger에서 업로드 테스트 성공 | 업로드화면 로컬 렌더링 성공 |

## 목요일

| 시간 | ① OCR·정보추출 | ② RAG·가이드생성 | ③ 챗봇·백엔드 | ④ 프론트·배포 |
|---|---|---|---|---|
| 오전 | OCR 인식 실패 케이스(흐릿한 이미지 등) 처리 로직 설계 | 복약가이드 생성 프롬프트 v1 초안 작성 | 챗봇 엔드포인트 스켈레톤 작성 | Docker Compose 초안 작성 시작(backend 서비스 정의) |
| 오후 | REQ-008 기준대로 에러메시지 반환 구현·테스트 | 약품명 1개 입력 → 가이드문 출력 테스트, 결과 다듬기 | asyncio 기반 비동기 처리 적용, 더미 응답으로 200 확인 | DB 서비스까지 Compose에 추가, `docker-compose up` 테스트 |
| **완료기준** | 실패 이미지 입력 시 에러메시지 정상 반환 | 가이드문 1건 정상 출력 확인 | 챗봇 엔드포인트 200 응답 확인 | 컨테이너 정상 기동 확인 |

## 금요일

| 시간 | ① OCR·정보추출 | ② RAG·가이드생성 | ③ 챗봇·백엔드 | ④ 프론트·배포 |
|---|---|---|---|---|
| 오전 | ②에 넘길 출력 포맷 최종 정리, ②와 1:1 합의 | ①의 출력 포맷 받아서 연결 테스트 준비 | ①②와 인터페이스 자리(스키마) 비워두고 API명세서 최신화 | 4명 코드 로컬 pull, merge 전 충돌 여부 사전 확인 |
| 오후 | ①→② end-to-end 연결 테스트 참여 | ①→② 입력→가이드생성 end-to-end 1건 성공 확인 | API명세서 vs Swagger 일치 여부 점검 | develop 브랜치로 4명 1차 통합(merge), 충돌 해결 |
| **완료기준** | ①→② 연결 테스트 1건 성공 | 〃 | API명세서-Swagger 일치 확인 | develop 브랜치 merge 충돌 없이 완료 |

---

## 1주차 전체 완료기준 (금요일 종료 시점)

- API 계약(JSON 스키마)이 ①→②→③→④ 전체에 문서화되어 있음
- 4명 모두 본인 모듈을 로컬에서 단독 실행 가능
- develop 브랜치 1차 통합 merge 충돌 없이 완료
