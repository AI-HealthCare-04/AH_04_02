# rag/.env — 이 파일을 복사해서 rag/.env로 만들고 값을 채우세요.
# backend/.env의 RAG_PROVIDER=real 또는 CHAT_PROVIDER=real일 때만 필요합니다
# (미설정 시 backend가 조용히 stub/mock으로 폴백합니다 — 에러 안 남).

# 식약처_의약품개요정보(e약은요) API 서비스키 — data.go.kr에서 발급
DATA_GO_KR_SERVICE_KEY=

# OpenAI API 키 — 복약 가이드 생성(RAG) + 챗봇 응답에 사용
OPENAI_API_KEY=
